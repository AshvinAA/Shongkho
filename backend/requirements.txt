# --- Runtime ---
fastapi
uvicorn[standard]
sqlalchemy
pymysql          # TiDB/MySQL driver
cryptography     # required by pymysql for SSL
python-dotenv
pydantic
bcrypt           # password hashing
itsdangerous     # signed session cookies
python-multipart # file uploads

# --- Analytics (Track A pipeline) ---
celery[redis]    # distributed task queue for the run pipeline
redis            # broker/backend client (also used for caching later)

# --- Testing ---
pytest
pytest-asyncio
httpx            # ASGI test client for FastAPI
